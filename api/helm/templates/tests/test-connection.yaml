apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webresourcecatalogus.fullname" . }}-test-connection"
  labels:
    {{- include "webresourcecatalogus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webresourcecatalogus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
